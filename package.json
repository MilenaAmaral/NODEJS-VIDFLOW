{
  "name": "nodejs-vidflow-vite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build-preview": "vite build --watch",
    "preview": "vite preview",
    "api-local": "json-server --watch backend/videos.json",
    "api": "json-server backend/videos.json --host 0.0.0.0 --port $PORT",
    "checar-codigo": "eslint ."
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "eslint": "^8.57.1",
    "prettier": "3.9.6"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "json-server": "^0.17.4"
  }
}